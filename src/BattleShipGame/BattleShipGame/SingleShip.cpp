#include <iostream>
#include "Ship.h"

class SingleShip : public Ship
{
	virtual StepResult& searchInBody(Ñoordinates coordinates) override
	{
		for(int i = 0; i < _body)
		return Boner();
	}

	virtual void drawBody() override
	{

	}
}